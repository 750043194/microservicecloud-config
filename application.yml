spring:
  profiles:
    active: dev

---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-atguigu-dev
---
spring:
  profiles: prod
  application:
    name: microservicecloud-config-atguigu-test
#注意:保存格式必须得是UTF-8
